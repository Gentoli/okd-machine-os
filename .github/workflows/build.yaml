on:
  push: {}

permissions:
  contents: 'read'
  id-token: 'write'

jobs:
  build:
    runs-on:
      - self-hosted
    container:
      image: us.gcr.io/gen-pub/okd-coreos-assembler:latest
    steps:
      - name: Yesss2
        shell: bash
        run: echo Yesss2
